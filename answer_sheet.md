# 資料結構作業一：答案紙

學號：114703059　姓名：李秉澄

轉成 PDF 後最多 2 頁。第二、三節由你自行撰寫的說明文字，合計以 500 個中文字以內為原則，不含題目敘述與表格中的純數值，不必寫滿。

---

## 一、容量擴充錯誤的最短反例

### 1. 最短操作序列

初始容量為 2。請填入最多 5 次 `enqueue()`／`dequeue()`：

```c++
q.enqueue(9);
q.dequeue();
q.enqueue(5);
q.enqueue(4);
q.enqueue(6); // 觸發grow

```

### 2. 逐步狀態

| 操作 | `front_` | `rear_` | `size_` | 邏輯順序 | 陣列實際排列 |
|---|---:|---:|---:|---|---|
| `q.enqueue(9);` | 0 | 1 | 1 | 9 | 9X |
| `q.dequeue();` | 1 | 1 | 0 | `empty` | XX |
| `q.enqueue(5);` | 1 | 0 | 1 | 5 | X5 |
| `q.enqueue(4);` | 1 | 1 | 2 | 54 | 45 |
| `q.enqueue(6);` | 1 | 0 | 3 | 546 | 46 |

### 3. 正確與錯誤結果

- 正確實作依序取出的元素：64
- 錯誤實作依序取出的元素：546

### 4. 被破壞的不變條件

佇列FIFO功能無法正確運作，enqueue將舊值覆蓋

---

## 二、操作次數與成長趨勢

本小節不需使用 Big-O 符號。

### 1. `CircularQueue`

- 計時開始時，`size()` 與 `capacity()` 各是多少？［請填入］`0, 4`
- 每輪先執行 `dequeue()` 再執行 `enqueue()`。兩個操作各會搬移多少個既有元素？［請填入］0
- 為什麼這段計時過程不會觸發容量擴充？［請填入］因為size_沒有大於等於capacity

### 2. `NaiveShiftQueue`

- 原有 `n` 個元素時，每次 `dequeue()` 約需向前搬移多少個元素？［請填入］n-1個
- 連續執行 `n` 輪後，總共約需搬移多少次？［請填入］n*(n-1)/2次

### 3. 資料規模增加後的成長

| 實作 | 當 `n` 增加為 8 倍時，基本操作次數約增加為幾倍？ | 推導方式 |
|---|---:|---|
| `CircularQueue` | 1 | 改變指標即可 |
| `NaiveShiftQueue` | 8 | dequeue後方元素皆須往前一格 |

---

## 三、實驗結果呈現與說明

### 1. 中位執行時間與成長倍率

| `n` | `CircularQueue` 中位時間（ms） | 與前一規模相比 | `NaiveShiftQueue` 中位時間（ms） | 與前一規模相比 |
|---:|---:|---:|---:|---:|
| $2^9$ |  | 不適用 |  | 不適用 |
| $2^{12}$ |  |  |  |  |
| $2^{15}$ |  |  |  |  |

### 2. 操作次數與實驗時間比較

將實測的時間成長倍率，與上一節根據操作次數推導出的倍率進行比較。實測結果是否符合相同的成長趨勢？若不完全一致，請提出一個合理解釋。可以簡短提到固定成本、記憶體存取效率或編譯器最佳化，不必深入討論硬體細節。

［請填入］
